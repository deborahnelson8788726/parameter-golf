{
	admin off
	auto_https off
}

:{$PORT:8080} {
	root * /srv
	encode gzip
	file_server

	# Clean 404 -> send people to the dashboard
	handle_errors {
		@404 expression {http.error.status_code} == 404
		redir @404 / 302
	}
}
